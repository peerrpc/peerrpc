// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: peerrpc/signaling/signaling.proto

package signalingpbconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	signaling "github.com/peerrpc/go/gen/proto/peerrpc/signaling"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SignalingServiceName is the fully-qualified name of the SignalingService service.
	SignalingServiceName = "peerrpc.signaling.SignalingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SignalingServiceExchangeProcedure is the fully-qualified name of the SignalingService's Exchange
	// RPC.
	SignalingServiceExchangeProcedure = "/peerrpc.signaling.SignalingService/Exchange"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	signalingServiceServiceDescriptor        = signaling.File_peerrpc_signaling_signaling_proto.Services().ByName("SignalingService")
	signalingServiceExchangeMethodDescriptor = signalingServiceServiceDescriptor.Methods().ByName("Exchange")
)

// SignalingServiceClient is a client for the peerrpc.signaling.SignalingService service.
type SignalingServiceClient interface {
	// Exchange is a bidirectional stream. A client announces itself
	// against a service and then exchanges offer / answer / candidate
	// messages with other peers in the same service until either side
	// closes the stream or the service entry is destroyed.
	Exchange(context.Context) *connect.BidiStreamForClient[signaling.SignalMessage, signaling.SignalMessage]
}

// NewSignalingServiceClient constructs a client for the peerrpc.signaling.SignalingService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSignalingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SignalingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &signalingServiceClient{
		exchange: connect.NewClient[signaling.SignalMessage, signaling.SignalMessage](
			httpClient,
			baseURL+SignalingServiceExchangeProcedure,
			connect.WithSchema(signalingServiceExchangeMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// signalingServiceClient implements SignalingServiceClient.
type signalingServiceClient struct {
	exchange *connect.Client[signaling.SignalMessage, signaling.SignalMessage]
}

// Exchange calls peerrpc.signaling.SignalingService.Exchange.
func (c *signalingServiceClient) Exchange(ctx context.Context) *connect.BidiStreamForClient[signaling.SignalMessage, signaling.SignalMessage] {
	return c.exchange.CallBidiStream(ctx)
}

// SignalingServiceHandler is an implementation of the peerrpc.signaling.SignalingService service.
type SignalingServiceHandler interface {
	// Exchange is a bidirectional stream. A client announces itself
	// against a service and then exchanges offer / answer / candidate
	// messages with other peers in the same service until either side
	// closes the stream or the service entry is destroyed.
	Exchange(context.Context, *connect.BidiStream[signaling.SignalMessage, signaling.SignalMessage]) error
}

// NewSignalingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSignalingServiceHandler(svc SignalingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	signalingServiceExchangeHandler := connect.NewBidiStreamHandler(
		SignalingServiceExchangeProcedure,
		svc.Exchange,
		connect.WithSchema(signalingServiceExchangeMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/peerrpc.signaling.SignalingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SignalingServiceExchangeProcedure:
			signalingServiceExchangeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSignalingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSignalingServiceHandler struct{}

func (UnimplementedSignalingServiceHandler) Exchange(context.Context, *connect.BidiStream[signaling.SignalMessage, signaling.SignalMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("peerrpc.signaling.SignalingService.Exchange is not implemented"))
}
